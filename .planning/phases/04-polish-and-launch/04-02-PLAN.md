---
phase: 04-polish-and-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/BookCard.astro
  - src/components/PodcastCard.astro
autonomous: true
requirements:
  - FOUN-04

must_haves:
  truths:
    - "Book and podcast cover images have explicit width and height preventing layout shift"
    - "Cover images load lazily to avoid blocking initial page render"
    - "Pages with cover images meet LCP < 2.5s target"
  artifacts:
    - path: "src/components/BookCard.astro"
      provides: "Book cover with Astro Image component and explicit dimensions"
      contains: "import { Image }"
    - path: "src/components/PodcastCard.astro"
      provides: "Podcast cover with Astro Image component and explicit dimensions"
      contains: "import { Image }"
  key_links:
    - from: "src/components/BookCard.astro"
      to: "public/assets/books/*.jpg"
      via: "Image component src prop with /assets/books/ path"
      pattern: "Image.*src.*coverImage"
    - from: "src/components/PodcastCard.astro"
      to: "public/assets/podcasts/*.jpg"
      via: "Image component src prop with /assets/podcasts/ path"
      pattern: "Image.*src.*coverImage"
---

<objective>
Convert BookCard and PodcastCard cover images from plain `<img>` to Astro `<Image>` component with explicit width/height for CLS prevention and lazy loading.

Purpose: Prevent Cumulative Layout Shift (CLS) by providing explicit dimensions on cover images. Supports FOUN-04 (LCP < 2.5s) through CLS prevention and lazy loading. Note: images in `/public/` are NOT optimized to WebP/AVIF by Astro — `<Image>` provides dimension enforcement and alt text enforcement only.

Output: Updated BookCard.astro and PodcastCard.astro using Astro `<Image>` component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-polish-and-launch/04-RESEARCH.md

<interfaces>
<!-- Current component interfaces -->

From src/components/BookCard.astro:
```astro
interface Props {
  title: string;
  author: string;
  coverImage?: string;
  status: 'reading' | 'read' | 'want-to-read';
}
```
Current img: `<img src={coverImage} alt={title} loading="lazy" class="w-full aspect-[2/3] object-cover rounded-t-md" />`

From src/components/PodcastCard.astro:
```astro
interface Props {
  name: string;
  link: string;
  coverImage?: string;
  category?: string;
}
```
Current img: `<img src={coverImage} alt={name} loading="lazy" class="w-16 h-16 rounded-md object-cover flex-shrink-0" />`

Cover image paths in content files: `/assets/books/*.jpg` and `/assets/podcasts/*.jpg` (public directory)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert BookCard and PodcastCard to Astro Image component</name>
  <read_first>
    - src/components/BookCard.astro
    - src/components/PodcastCard.astro
    - .planning/phases/04-polish-and-launch/04-RESEARCH.md (Pattern 5: Astro Image with /public/ Assets)
  </read_first>
  <files>src/components/BookCard.astro, src/components/PodcastCard.astro</files>
  <action>
    IMPORTANT CONTEXT: Images are in `/public/assets/` and Astro's `<Image>` does NOT convert them to WebP/AVIF. The `<Image>` component is used here for CLS prevention (explicit width/height per D-10) and lazy loading (per D-11). This is intentional — moving images to `src/assets/` would require changing all content frontmatter paths and Keystatic schema directory fields.

    **BookCard.astro:**
    1. Add import at top of frontmatter: `import { Image } from 'astro:assets';`
    2. Replace the `<img>` tag in the conditional with:
       ```astro
       <Image
         src={coverImage}
         alt={title}
         width={200}
         height={300}
         loading="lazy"
         class="w-full aspect-[2/3] object-cover rounded-t-md"
       />
       ```
    3. Keep the placeholder `<div>` fallback for when coverImage is undefined — unchanged
    4. Keep the Props interface unchanged — `coverImage?: string` is compatible with `<Image src={string}>`
    5. Preserve ALL existing Tailwind classes and the rest of the component markup exactly as-is
    6. BookCard dimensions: width=200, height=300 (2:3 aspect ratio matching the existing `aspect-[2/3]` class) per D-10

    **PodcastCard.astro:**
    1. Add import at top of frontmatter: `import { Image } from 'astro:assets';`
    2. Replace the `<img>` tag in the conditional with:
       ```astro
       <Image
         src={coverImage}
         alt={name}
         width={64}
         height={64}
         loading="lazy"
         class="w-16 h-16 rounded-md object-cover flex-shrink-0"
       />
       ```
    3. Keep the placeholder `<div>` fallback — unchanged
    4. Keep the Props interface unchanged
    5. Preserve ALL existing Tailwind classes and the rest of the component markup exactly as-is
    6. PodcastCard dimensions: width=64, height=64 (matching existing `w-16 h-16` = 64px) per D-10
  </action>
  <verify>
    <automated>npm run build && grep -q "import { Image }" src/components/BookCard.astro && grep -q "import { Image }" src/components/PodcastCard.astro && echo "PASS: Image components converted"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/BookCard.astro` contains `import { Image } from 'astro:assets'`
    - `src/components/BookCard.astro` contains `<Image` (not `<img` for cover images)
    - `src/components/BookCard.astro` contains `width={200}`
    - `src/components/BookCard.astro` contains `height={300}`
    - `src/components/BookCard.astro` contains `loading="lazy"`
    - `src/components/PodcastCard.astro` contains `import { Image } from 'astro:assets'`
    - `src/components/PodcastCard.astro` contains `<Image` (not `<img` for cover images)
    - `src/components/PodcastCard.astro` contains `width={64}`
    - `src/components/PodcastCard.astro` contains `height={64}`
    - `src/components/PodcastCard.astro` contains `loading="lazy"`
    - Placeholder div fallback still present in both components (for undefined coverImage)
    - `npm run build` succeeds with no errors
    - Built `/books` and `/podcasts` pages contain `width="200"` and `width="64"` respectively in their HTML output
  </acceptance_criteria>
  <done>BookCard uses Astro Image with width=200 height=300, PodcastCard uses Astro Image with width=64 height=64, both have loading="lazy", build passes, CLS prevented by explicit dimensions per D-10.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. BookCard.astro and PodcastCard.astro both use `<Image>` from `astro:assets`
3. Built HTML for /books page contains `width="200"` and `height="300"` on cover images
4. Built HTML for /podcasts page contains `width="64"` and `height="64"` on cover images
5. Placeholder divs still render for entries without cover images
</verification>

<success_criteria>
- FOUN-04 (partial): CLS prevented on book and podcast pages via explicit image dimensions. LCP target supported by lazy loading below-fold images. Full LCP verification happens post-deployment.
</success_criteria>

<output>
After completion, create `.planning/phases/04-polish-and-launch/04-02-SUMMARY.md`
</output>
