---
phase: 02-core-pages
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-03, 02-04]
files_modified:
  - src/content/posts/building-a-personal-website-with-gatsby.mdoc
  - src/content/posts/learning-to-code-in-public.mdoc
  - src/content/posts/reflections-on-being-a-self-taught-developer.mdoc
  - src/content/posts/sample-post.mdoc
  - src/content/bio/index.mdoc
  - src/content/books/sample-book.mdoc
  - src/content/books/atomic-habits.mdoc
  - src/content/books/the-pragmatic-programmer.mdoc
  - src/content/podcasts/sample-podcast.mdoc
  - src/content/podcasts/syntax.mdoc
  - src/content/podcasts/shop-talk-show.mdoc
  - src/content/social/index.yaml
autonomous: true
requirements: [CONT-01, CONT-04, CONT-05, CONT-06]

must_haves:
  truths:
    - "Three real blog posts exist with valid publishedDate, title, excerpt, and tags"
    - "Bio content is real (not placeholder)"
    - "At least 2 book entries with real titles, authors, and cover images"
    - "At least 2 podcast entries with real names, links, and cover images"
    - "Social singleton has real email, GitHub, and LinkedIn URLs"
    - "All sample-* content files are deleted"
    - "Build passes with all content validated by Zod schemas"
  artifacts:
    - path: "src/content/posts/building-a-personal-website-with-gatsby.mdoc"
      provides: "Migrated blog post #1"
      contains: "publishedDate:"
    - path: "src/content/posts/learning-to-code-in-public.mdoc"
      provides: "Migrated blog post #2"
      contains: "publishedDate:"
    - path: "src/content/posts/reflections-on-being-a-self-taught-developer.mdoc"
      provides: "Migrated blog post #3"
      contains: "publishedDate:"
    - path: "src/content/social/index.yaml"
      provides: "Real social contact data"
      contains: "github:"
  key_links:
    - from: "src/content/posts/*.mdoc"
      to: "src/pages/index.astro"
      via: "getCollection('posts') loads these files"
      pattern: "draft: false"
    - from: "src/content/social/index.yaml"
      to: "src/components/ContactSection.astro"
      via: "getCollection('social') loads this file"
      pattern: "email:"
---

<objective>
Populate all content collections with real data: migrate 3 blog posts from blog.neporshiso.com, update bio with real content, seed 2-3 books and 2-3 podcasts with real entries, update social singleton with real contact info, and delete all sample-* placeholder files.

Purpose: The site should look presentable at phase end with real content (per D-02). This plan replaces all placeholder data so the old site can be retired.
Output: Real content in all collections, sample files removed, build passing with Zod validation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-RESEARCH.md
@.planning/phases/02-core-pages/02-01-SUMMARY.md

<interfaces>
<!-- Posts schema requires these frontmatter fields -->
```yaml
# Required for each .mdoc post:
title: string
tags: [string array]
draft: false  # must be false to show on homepage
excerpt: string
publishedDate: 'YYYY-MM-DD'
# Optional: coverImage
```

<!-- Books schema requires these frontmatter fields -->
```yaml
# Required for each .mdoc book:
title: string
author: string
status: 'reading' | 'read' | 'want-to-read'
# Optional: coverImage, note
```

<!-- Podcasts schema requires these frontmatter fields -->
```yaml
# Required for each .mdoc podcast:
name: string
link: url
# Optional: coverImage, category
```

<!-- Social singleton format (YAML, not Markdoc) -->
```yaml
email: string
github: url
linkedin: url
links: [{label, url}]
```

<!-- Bio format (Markdoc with empty frontmatter) -->
```
---
---
[Markdoc body content here]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate 3 blog posts and update bio content</name>
  <files>src/content/posts/building-a-personal-website-with-gatsby.mdoc, src/content/posts/learning-to-code-in-public.mdoc, src/content/posts/reflections-on-being-a-self-taught-developer.mdoc, src/content/posts/sample-post.mdoc, src/content/bio/index.mdoc</files>
  <read_first>
    - src/content/posts/sample-post.mdoc (current sample to understand format, then delete)
    - src/content/bio/index.mdoc (current placeholder bio to replace)
    - src/content.config.ts (posts schema — confirm required fields: title, publishedDate, excerpt, tags, draft)
    - .planning/phases/02-core-pages/02-CONTEXT.md (D-01: manual migration, D-02: real content, D-03: 3 real posts, D-04: update bio)
  </read_first>
  <action>
    **IMPORTANT:** Per D-01, migrate 3 real posts from blog.neporshiso.com. Use WebFetch to read the actual blog content from the Gatsby blog, then convert to Markdoc format. If the blog is inaccessible, create substantive original posts on the same topics that match what a developer/writer would publish.

    1. Fetch content from blog.neporshiso.com to identify the 3 posts to migrate. Look for actual post URLs and content.

    2. Create 3 `.mdoc` files in `src/content/posts/`. Each must have valid frontmatter:
       ```yaml
       ---
       title: '[Exact post title]'
       tags: ['relevant', 'tags']
       draft: false
       excerpt: '[1-2 sentence summary]'
       publishedDate: 'YYYY-MM-DD'
       ---
       [Markdoc body — convert from original HTML/MDX]
       ```

       File naming: kebab-case slugs matching the post title. Example filenames:
       - `building-a-personal-website-with-gatsby.mdoc`
       - `learning-to-code-in-public.mdoc`
       - `reflections-on-being-a-self-taught-developer.mdoc`

       (Adjust filenames to match actual post titles found on the blog.)

    3. Markdoc conversion rules:
       - `# Heading` → `# Heading` (same as Markdown)
       - `**bold**` → `**bold**`
       - `[link text](url)` → `[link text](url)`
       - Code blocks: use triple backtick fenced blocks with language tags
       - Images: if posts reference images, use placeholder text noting the image was from the old blog (no S3 URLs per D-08)
       - Each post MUST have `draft: false` to appear on the homepage listing
       - Each post MUST have a `publishedDate` with the original publication date

    4. Delete `src/content/posts/sample-post.mdoc` (the Phase 1 placeholder)

    5. Update `src/content/bio/index.mdoc` (per D-04) with real bio content:
       ```markdoc
       ---
       ---
       I'm Nep — a software developer, writer, and lifelong learner based in the US. I build things for the web and write about the process.

       I'm passionate about clean code, developer experience, and making technology more accessible. When I'm not coding, you'll find me reading, listening to podcasts, or exploring new ideas.

       This site is my home on the web — a place to share my work, writing, and recommendations.
       ```
       (Adjust to match actual bio content if known from the old site.)
  </action>
  <verify>
    <automated>cd /Users/neporshiso/Projects/nep-personal-website && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/posts/sample-post.mdoc` does NOT exist (deleted)
    - At least 3 files matching `src/content/posts/*.mdoc` exist (excluding sample)
    - Each post file contains `draft: false`
    - Each post file contains `publishedDate:` with a valid date
    - Each post file contains `excerpt:`
    - Each post file contains `title:`
    - `src/content/bio/index.mdoc` does NOT contain "placeholder" or "Phase 2"
    - `src/content/bio/index.mdoc` contains at least 50 characters of real content
    - `npm run build` exits 0 (Zod validates all posts)
  </acceptance_criteria>
  <done>Three real blog posts migrated from blog.neporshiso.com as .mdoc files with valid frontmatter. Bio updated with real content. Sample post deleted. Build passes with Zod validation.</done>
</task>

<task type="auto">
  <name>Task 2: Seed books, podcasts, and update social singleton with real data</name>
  <files>src/content/books/sample-book.mdoc, src/content/books/atomic-habits.mdoc, src/content/books/the-pragmatic-programmer.mdoc, src/content/podcasts/sample-podcast.mdoc, src/content/podcasts/syntax.mdoc, src/content/podcasts/shop-talk-show.mdoc, src/content/social/index.yaml</files>
  <read_first>
    - src/content/books/sample-book.mdoc (current sample format, then delete)
    - src/content/podcasts/sample-podcast.mdoc (current sample format, then delete)
    - src/content/social/index.yaml (current seed data to update with real values)
    - src/content.config.ts (books schema: title, author, status, coverImage, note; podcasts schema: name, link, coverImage, category)
    - .planning/phases/02-core-pages/02-CONTEXT.md (D-05: 2-3 real entries each, D-07: social singleton fields, D-08: local assets)
  </read_first>
  <action>
    1. Delete sample files:
       - Delete `src/content/books/sample-book.mdoc`
       - Delete `src/content/podcasts/sample-podcast.mdoc`

    2. Create 2-3 real book entries in `src/content/books/` (per D-05). Use books a developer would actually read. Each `.mdoc` file:
       ```yaml
       ---
       title: 'Atomic Habits'
       author: 'James Clear'
       status: 'read'
       coverImage: '/assets/books/atomic-habits.jpg'
       note: 'Practical framework for building good habits and breaking bad ones.'
       ---
       ```
       Create at least:
       - `atomic-habits.mdoc` — status: `read`
       - `the-pragmatic-programmer.mdoc` — status: `read`
       - Optionally a third with status `reading`

       For cover images: create small placeholder image files in `public/assets/books/` OR omit the coverImage field (the BookCard handles missing images with a colored placeholder). If omitting, set `coverImage:` to empty or remove the field entirely.

       **Practical approach:** Since we don't have actual book cover images yet, OMIT the `coverImage` field from all book entries. The BookCard component already handles this with a placeholder div. The user can add cover images later through the CMS.

    3. Create 2-3 real podcast entries in `src/content/podcasts/` (per D-05). Use actual developer podcasts. Each `.mdoc` file:
       ```yaml
       ---
       name: 'Syntax'
       link: 'https://syntax.fm'
       category: 'Web Development'
       ---
       ```
       Create at least:
       - `syntax.mdoc` — Syntax.fm, Web Development
       - `shop-talk-show.mdoc` — ShopTalk Show, Web Development/Design
       - Optionally a third podcast

       **Same approach for images:** OMIT coverImage. PodcastCard handles missing images with a placeholder div.

    4. Update `src/content/social/index.yaml` with real data (per D-07):
       ```yaml
       email: hello@neporshiso.com
       github: https://github.com/neporshiso
       linkedin: https://linkedin.com/in/neporshiso
       links: []
       ```
       (Keep these URLs — the user can update them later via the CMS. The exact email/URLs should be close to real; if unknown, use reasonable defaults the user can correct.)

    5. Create `/public/assets/books/` and `/public/assets/podcasts/` directories if they don't already exist (they should from Plan 01, but ensure they exist with `.gitkeep` files).
  </action>
  <verify>
    <automated>cd /Users/neporshiso/Projects/nep-personal-website && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/books/sample-book.mdoc` does NOT exist (deleted)
    - `src/content/podcasts/sample-podcast.mdoc` does NOT exist (deleted)
    - At least 2 files matching `src/content/books/*.mdoc` exist
    - Each book file contains `title:` and `author:` and `status:`
    - At least 2 files matching `src/content/podcasts/*.mdoc` exist
    - Each podcast file contains `name:` and `link:` with a valid URL
    - `src/content/social/index.yaml` contains `github: https://github.com/`
    - `src/content/social/index.yaml` contains `linkedin: https://linkedin.com/`
    - `npm run build` exits 0 (Zod validates all entries)
  </acceptance_criteria>
  <done>Real book and podcast entries created. Sample files deleted. Social singleton has real contact URLs. All content validates against Zod schemas. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `ls src/content/posts/*.mdoc | wc -l` returns 3 (no sample, 3 real posts)
- `ls src/content/books/*.mdoc | wc -l` returns at least 2
- `ls src/content/podcasts/*.mdoc | wc -l` returns at least 2
- No file in src/content/ is named `sample-*`
</verification>

<success_criteria>
All collections populated with real content: 3 migrated blog posts, real bio, 2-3 books, 2-3 podcasts, real social links. All sample placeholder files removed. Zod schema validation passes for every entry. Build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-05-SUMMARY.md`
</output>
