---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - keystatic.config.ts
  - src/content.config.ts
  - src/content/projects/sample-project.mdoc
  - src/content/posts/sample-post.mdoc
  - src/content/podcasts/sample-podcast.mdoc
  - src/content/books/sample-book.mdoc
  - src/content/bio/index.mdoc
  - src/pages/about.astro
autonomous: false
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-03

must_haves:
  truths:
    - "All five content type schemas (Project, Post, Bio, Podcast, Book) are defined in both keystatic.config.ts and src/content.config.ts"
    - "Keystatic admin UI at /keystatic shows all five content types and allows entry creation"
    - "getCollection() returns typed data for each collection with no TypeScript errors"
    - "Malformed content entries are rejected at build time by Zod validation"
    - "At least one sample entry exists per collection so getCollection() returns data"
    - "The about page renders bio content from CMS via getCollection()"
  artifacts:
    - path: "keystatic.config.ts"
      provides: "CMS schema for all five content types"
      contains: "projects"
    - path: "src/content.config.ts"
      provides: "Astro Zod schemas matching Keystatic schemas"
      exports: ["collections"]
    - path: "src/content/projects/sample-project.mdoc"
      provides: "Sample project content entry"
      contains: "title"
    - path: "src/content/posts/sample-post.mdoc"
      provides: "Sample blog post content entry"
      contains: "title"
    - path: "src/content/bio/index.mdoc"
      provides: "Singleton bio content"
      contains: "bio"
    - path: "src/pages/about.astro"
      provides: "About page that reads bio via getCollection"
      contains: "getCollection"
  key_links:
    - from: "keystatic.config.ts"
      to: "src/content/"
      via: "Keystatic writes content files to src/content/{collection}/"
      pattern: "path: 'src/content/"
    - from: "src/content.config.ts"
      to: "src/content/"
      via: "Astro glob loader reads content files with Zod validation"
      pattern: "glob.*base.*src/content/"
    - from: "src/pages/about.astro"
      to: "src/content.config.ts"
      via: "getCollection('bio') reads validated bio content"
      pattern: "getCollection.*bio"
---

<objective>
Define all five content type schemas in both Keystatic and Astro configs, create sample content entries, and verify the CMS-to-page pipeline works end-to-end.

Purpose: Content schemas are the contract between the CMS (where content is edited) and Astro pages (where content is displayed). Without these schemas, no content page can be built in Phase 2+. This plan proves the pipeline works by rendering a real CMS entry on a real page.
Output: keystatic.config.ts with all 5 content types, src/content.config.ts with matching Zod schemas, sample entries for each collection, and an about page that renders the bio singleton via getCollection().
</objective>

<execution_context>
@/Users/neporshiso/.claude/get-shit-done/workflows/execute-plan.md
@/Users/neporshiso/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Keystatic config and Astro content schemas for all five content types</name>
  <files>
    keystatic.config.ts,
    src/content.config.ts
  </files>
  <read_first>
    .planning/phases/01-foundation/01-RESEARCH.md (Keystatic Config: All Five Content Types code example, Astro Content Config: Matching Zod Schemas code example, Pattern 4: Dual Schema),
    .planning/phases/01-foundation/01-CONTEXT.md (Content schema fields section — exact field names and types for each content type),
    astro.config.mjs (verify Keystatic integration is configured)
  </read_first>
  <action>
    1. Create keystatic.config.ts at project root with ALL five content type schemas. Use the exact code from 01-RESEARCH.md "Keystatic Config: All Five Content Types" section with these specifications:

       **Collections:**

       **projects** — path: 'src/content/projects/*', slugField: 'title'
       - title: fields.slug({ name: { label: 'Title' } })
       - description: fields.markdoc({ label: 'Description' })
       - techStack: fields.array(fields.text({ label: 'Technology' }), { label: 'Tech Stack', itemLabel: (props) => props.fields.value.value })
       - media: fields.url({ label: 'Media URL (S3)', validation: { isRequired: false } })
       - links: fields.array(fields.object({ label: fields.text({ label: 'Label' }), url: fields.url({ label: 'URL' }) }), { label: 'Links', itemLabel: (props) => props.fields.label.value })
       - year: fields.integer({ label: 'Year' })

       **posts** — path: 'src/content/posts/*', slugField: 'title', format: { contentField: 'body' }
       - title: fields.slug({ name: { label: 'Title' } })
       - body: fields.markdoc({ label: 'Body' })
       - tags: fields.array(fields.text({ label: 'Tag' }), { label: 'Tags', itemLabel: (props) => props.fields.value.value })
       - coverImage: fields.image({ label: 'Cover Image', directory: 'src/assets/images/posts', publicPath: '@assets/images/posts/' })
       - draft: fields.checkbox({ label: 'Draft', defaultValue: true })
       - excerpt: fields.text({ label: 'Excerpt', multiline: true })
       - publishedDate: fields.date({ label: 'Published Date' })

       **podcasts** — path: 'src/content/podcasts/*', slugField: 'name'
       - name: fields.slug({ name: { label: 'Name' } })
       - link: fields.url({ label: 'Link' })
       - coverImage: fields.image({ label: 'Cover Image', directory: 'src/assets/images/podcasts', publicPath: '@assets/images/podcasts/' })
       - category: fields.text({ label: 'Category' })

       **books** — path: 'src/content/books/*', slugField: 'title'
       - title: fields.slug({ name: { label: 'Title' } })
       - author: fields.text({ label: 'Author' })
       - coverImage: fields.image({ label: 'Cover Image', directory: 'src/assets/images/books', publicPath: '@assets/images/books/' })
       - status: fields.select({ label: 'Status', options: [{ label: 'Reading', value: 'reading' }, { label: 'Read', value: 'read' }, { label: 'Want to Read', value: 'want-to-read' }], defaultValue: 'want-to-read' })
       - note: fields.text({ label: 'Note', multiline: true })

       **Singletons:**

       **bio** — path: 'src/content/bio', format: { contentField: 'body' }
       - body: fields.markdoc({ label: 'Bio Content' })

       Storage config: `storage: { kind: 'local' }`

    2. Create src/content.config.ts with matching Zod schemas. Use the exact code from 01-RESEARCH.md "Astro Content Config: Matching Zod Schemas" section. Add the bio collection that was missing from the research example:

       ```typescript
       const bio = defineCollection({
         loader: glob({ pattern: '**/*.mdoc', base: './src/content/bio' }),
         schema: z.object({}),
       });
       ```

       Export ALL collections: `export const collections = { projects, posts, podcasts, books, bio };`

       Add a comment at the top of each file: `// Schema must stay in sync with keystatic.config.ts / src/content.config.ts`

    3. Create the content directories if they don't exist:
       - src/content/projects/
       - src/content/posts/
       - src/content/podcasts/
       - src/content/books/
       - src/content/bio/
       - src/assets/images/posts/
       - src/assets/images/podcasts/
       - src/assets/images/books/
  </action>
  <verify>
    <automated>cd /Users/neporshiso/Projects/nep-personal-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - keystatic.config.ts exists at project root
    - keystatic.config.ts contains "storage: { kind: 'local' }"
    - keystatic.config.ts contains "projects: collection("
    - keystatic.config.ts contains "posts: collection("
    - keystatic.config.ts contains "podcasts: collection("
    - keystatic.config.ts contains "books: collection("
    - keystatic.config.ts contains "bio: singleton("
    - keystatic.config.ts contains "fields.slug"
    - keystatic.config.ts contains "fields.markdoc"
    - keystatic.config.ts contains "fields.select"
    - keystatic.config.ts contains "want-to-read"
    - src/content.config.ts exists
    - src/content.config.ts contains "defineCollection"
    - src/content.config.ts contains "z.object"
    - src/content.config.ts contains "glob"
    - src/content.config.ts contains "projects"
    - src/content.config.ts contains "posts"
    - src/content.config.ts contains "podcasts"
    - src/content.config.ts contains "books"
    - src/content.config.ts contains "bio"
    - src/content.config.ts contains "export const collections"
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All five content type schemas defined in both keystatic.config.ts and src/content.config.ts with matching fields, storage set to local, and build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: Create sample content entries and an about page that reads bio via getCollection</name>
  <files>
    src/content/projects/sample-project.mdoc,
    src/content/posts/sample-post.mdoc,
    src/content/podcasts/sample-podcast.mdoc,
    src/content/books/sample-book.mdoc,
    src/content/bio/index.mdoc,
    src/pages/about.astro
  </files>
  <read_first>
    keystatic.config.ts (verify exact field names and collection paths from Task 1),
    src/content.config.ts (verify Zod schema field names and types),
    src/layouts/BaseLayout.astro (verify layout props and import path),
    .planning/phases/01-foundation/01-RESEARCH.md (Using getCollection() in an Astro Page example)
  </read_first>
  <action>
    1. Create sample content entries in Markdoc format. Each file needs YAML frontmatter matching the Zod schema, plus Markdoc body content where applicable.

       **src/content/projects/sample-project.mdoc:**
       ```
       ---
       title: Sample Project
       techStack:
         - TypeScript
         - Astro
       media: ~
       links:
         - label: GitHub
           url: https://github.com
       year: 2024
       ---
       This is a sample project description for testing the content pipeline.
       ```

       **src/content/posts/sample-post.mdoc:**
       ```
       ---
       title: Sample Post
       tags:
         - test
       draft: false
       excerpt: A sample blog post for testing the content pipeline.
       publishedDate: '2024-01-15'
       ---
       This is the body of a sample blog post. It exists to verify that `getCollection('posts')` returns data.
       ```

       **src/content/podcasts/sample-podcast.mdoc:**
       ```
       ---
       name: Sample Podcast
       link: https://example.com/podcast
       category: Technology
       ---
       ```

       **src/content/books/sample-book.mdoc:**
       ```
       ---
       title: Sample Book
       author: Sample Author
       status: read
       note: A sample book entry for testing.
       ---
       ```

       **src/content/bio/index.mdoc:**
       ```
       ---
       ---
       Hi, I'm Nep. This is a placeholder bio that will be replaced with real content in Phase 2.
       ```

    2. Create src/pages/about.astro that reads the bio singleton:
       ```astro
       ---
       import { getCollection } from 'astro:content';
       import BaseLayout from '../layouts/BaseLayout.astro';

       const bioEntries = await getCollection('bio');
       const bio = bioEntries[0];
       ---
       <BaseLayout title="About - Nep">
         <h1 class="text-3xl font-bold mb-6">About</h1>
         {bio ? (
           <div class="prose">
             <Content />
           </div>
         ) : (
           <p>Bio content coming soon.</p>
         )}
       </BaseLayout>
       ```
       Note: For rendering Markdoc content, use the `render()` method on the collection entry. The exact API depends on Astro's content rendering — check the entry object for a `render()` method that returns `{ Content }`. The pattern is:
       ```
       const { Content } = await bio.render();
       ```
       Then use `<Content />` in the template.

    3. Verify the build succeeds and getCollection returns data by checking the build output for no errors.
  </action>
  <verify>
    <automated>cd /Users/neporshiso/Projects/nep-personal-website && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/content/projects/sample-project.mdoc exists and contains "title: Sample Project"
    - src/content/posts/sample-post.mdoc exists and contains "title: Sample Post"
    - src/content/posts/sample-post.mdoc contains "publishedDate:"
    - src/content/podcasts/sample-podcast.mdoc exists and contains "name: Sample Podcast"
    - src/content/books/sample-book.mdoc exists and contains "title: Sample Book"
    - src/content/books/sample-book.mdoc contains "status: read"
    - src/content/bio/index.mdoc exists and contains "Nep"
    - src/pages/about.astro exists and contains "getCollection"
    - src/pages/about.astro contains "bio"
    - src/pages/about.astro contains "BaseLayout"
    - `npm run build` exits with code 0 (proves Zod validation accepted all sample entries)
  </acceptance_criteria>
  <done>Sample content entries exist for all five collections, Zod validation accepts them at build time, and the about page renders bio content via getCollection().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify full foundation stack works end-to-end</name>
  <files>N/A — verification only, no files modified</files>
  <read_first>
    src/pages/index.astro (verify index page exists for step 1-2),
    src/pages/about.astro (verify about page exists for step 8),
    src/layouts/BaseLayout.astro (verify layout structure for visual checks)
  </read_first>
  <action>
    Human verification checkpoint. No automated action — the executor presents the verification steps below to the user and waits for approval.

    What was built: Complete Phase 1 foundation — Astro 6 + Tailwind v4 + Keystatic CMS with all five content schemas, BaseLayout with dark/light mode toggle, responsive nav, and sample content entries.

    Steps for the user to verify:
    1. Run `npm run dev` and open http://localhost:4321
    2. Verify the index page renders with warm cream background (#fdf6e3) and dark text
    3. Click the theme toggle (sun/moon icon in nav) — verify instant switch to dark mode (#1d2021 background)
    4. Refresh the page — verify dark mode persists (no flash of light theme)
    5. Toggle back to light mode, refresh — verify light mode persists
    6. Open browser DevTools, set "prefers-color-scheme: dark" in Rendering tab — open a new tab to localhost:4321 — verify dark mode auto-detected
    7. Resize browser to 375px width — verify nav collapses to mobile menu and content is readable
    8. Navigate to http://localhost:4321/about — verify bio placeholder text renders
    9. Navigate to http://localhost:4321/keystatic — verify Keystatic admin UI loads
    10. In Keystatic admin, verify all five content types are listed (Projects, Posts, Podcasts, Books, Bio)
    11. Click into any collection — verify the sample entry is visible
  </action>
  <verify>User confirms all 11 verification steps pass by typing "approved"</verify>
  <acceptance_criteria>
    - User confirms warm cream background (#fdf6e3) visible in light mode
    - User confirms dark mode (#1d2021 background) activates on toggle click
    - User confirms theme persists across page refresh
    - User confirms no flash of wrong theme on load
    - User confirms mobile responsive layout at 375px
    - User confirms /about page renders bio text
    - User confirms /keystatic admin UI loads with all 5 content types
  </acceptance_criteria>
  <done>User has approved the full Phase 1 foundation stack — dark mode, responsive layout, CMS pipeline, and all content schemas verified working end-to-end.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with exit code 0
2. All five content collections have matching schemas in keystatic.config.ts and src/content.config.ts
3. Sample content entries pass Zod validation at build time
4. getCollection('bio') returns data on the about page
5. Keystatic admin at /keystatic shows all five content types
6. Dark mode toggle works with localStorage persistence
7. No FOUC on page load
8. Responsive layout works at 375px
</verification>

<success_criteria>
- All five content schemas defined and in sync between Keystatic and Astro configs
- Sample entries exist and pass build-time Zod validation
- About page renders bio content from CMS via getCollection()
- Keystatic admin UI accessible and functional at /keystatic
- Complete foundation verified by human: dark mode, responsive layout, CMS pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
