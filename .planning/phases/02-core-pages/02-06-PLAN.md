---
phase: 02-core-pages
plan: 06
type: execute
wave: 4
depends_on: [02-05]
files_modified: []
autonomous: false
requirements: [CONT-01, CONT-02, CONT-04, CONT-05, CONT-06, ANLY-01]

must_haves:
  truths:
    - "All 6 pages render correctly at 375px mobile width"
    - "Nav shows contextual site name per page"
    - "Dark mode toggle works across all pages"
    - "Blog posts appear on homepage sorted newest-first"
    - "About page shows bio content and social links"
    - "Portfolio page shows empty state"
    - "Podcasts page shows real podcast entries"
    - "Books page shows real book entries with status badges"
  artifacts: []
  key_links: []
---

<objective>
Full visual and functional smoke test of all core pages at 375px mobile width and desktop. Verify the entire Phase 2 works end-to-end: contextual nav, blog listing, about + social, portfolio empty state, podcasts, books, contact section, dark mode, and analytics script presence.

Purpose: Final checkpoint before phase verification — catch visual regressions and functional issues.
Output: Human approval or issue list for fixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-core-pages/02-CONTEXT.md
@.planning/phases/02-core-pages/02-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build and start preview server</name>
  <files></files>
  <read_first>
    - package.json (confirm build and preview scripts)
  </read_first>
  <action>
    Run a full production build and start the preview server:
    ```bash
    cd /Users/neporshiso/Projects/nep-personal-website
    npm run build
    ```
    Verify:
    - Build exits 0
    - All expected pages generated in `dist/`:
      - `dist/index.html` (homepage)
      - `dist/about/index.html`
      - `dist/portfolio/index.html`
      - `dist/podcasts/index.html`
      - `dist/books/index.html`
    - Check that the Analytics script tag is present in the built HTML:
      ```bash
      grep -l "vercel" dist/index.html
      ```
    - Start preview: `npm run preview` (runs in background for human verification)
  </action>
  <verify>
    <automated>cd /Users/neporshiso/Projects/nep-personal-website && npm run build && test -f dist/index.html && test -f dist/about/index.html && test -f dist/portfolio/index.html && test -f dist/podcasts/index.html && test -f dist/books/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `dist/index.html` exists
    - `dist/about/index.html` exists
    - `dist/portfolio/index.html` exists
    - `dist/podcasts/index.html` exists
    - `dist/books/index.html` exists
  </acceptance_criteria>
  <done>Production build succeeds, all 5 main pages generated, preview server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual and functional smoke test of all core pages</name>
  <files></files>
  <action>Human visually verifies all pages at desktop and 375px mobile width.</action>
  <what-built>All core pages: homepage (blog listing + contact), about (bio + social links), portfolio (empty state), podcasts (real entries), books (real entries with status badges). Contextual nav site names, dark mode, responsive layout, Vercel Analytics.</what-built>
  <how-to-verify>
    Start the dev server: `npm run dev` (or preview server: `npm run build && npm run preview`)

    **Desktop verification:**
    1. Visit http://localhost:4321/ — Homepage shows blog posts (newest-first), each with title/date/excerpt. ContactSection at bottom with "Get in touch" and social icons. Nav says "nep's home on the web".
    2. Visit http://localhost:4321/about — Bio text renders (not placeholder). Social links row with GitHub/LinkedIn/Email icons. Nav says "about nep".
    3. Visit http://localhost:4321/portfolio — Shows "No projects yet." empty state. Nav says "nep's portfolio".
    4. Visit http://localhost:4321/podcasts — Shows 2-3 podcast cards with name and category. Nav says "nep's favorite podcasts".
    5. Visit http://localhost:4321/books — Shows 2-3 book cards with title, author, status badge. Nav says "nep's reading".
    6. Toggle dark mode on each page — colors switch correctly, no flash on reload.
    7. Check active nav link — current page link should be accent colored.

    **Mobile verification (375px):**
    8. Open Chrome DevTools, set viewport to 375px width.
    9. Verify homepage blog entries stack vertically, no horizontal overflow.
    10. Verify hamburger menu opens and closes, links are tappable.
    11. Verify books grid shows 2 columns on mobile.
    12. Verify all text is readable, no truncation issues.

    **Analytics check:**
    13. Open Network tab in DevTools, look for requests to `/_vercel/insights/` (may only appear on actual Vercel deployment — check that the Analytics script tag is in the page source).
  </how-to-verify>
  <verify>Human confirms all 13 verification steps pass.</verify>
  <acceptance_criteria>
    - Human confirms all pages render at desktop and 375px mobile
    - Human confirms contextual nav site names are correct per page
    - Human confirms dark mode toggle works
    - Human confirms blog posts visible on homepage
    - Human confirms empty state on portfolio
  </acceptance_criteria>
  <done>Human approves visual and functional quality of all core pages.</done>
  <resume-signal>Type "approved" to complete Phase 2, or describe any issues found.</resume-signal>
</task>

</tasks>

<verification>
- Build passes
- All pages accessible and rendering correctly
- Human approves visual and functional quality
</verification>

<success_criteria>
Human confirms all 6 pages render correctly at both desktop and 375px mobile. Contextual nav works. Dark mode works. Blog posts visible. Social links functional. Empty states display correctly. No visual regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages/02-06-SUMMARY.md`
</output>
